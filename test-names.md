# Test names for review

1. `Cart is empty after checkout completes`
2. `getUserById returns null for unknown ids`
3. `Password reset token expires after 3600 seconds`
4. `${input} is a valid email`
5. `should update the cart total when an item is added`
6. `Retry stops after the third failed attempt`
7. `Renders`
8. `Discount is not applied twice for the same promo code`
9. `CheckoutService - applyDiscount - returns 0.9`
10. `HTTP 404 shows the not-found page`
11. `Marketing consent remains true after the user re-subscribes to the newsletter`
12. `Formats ISO 8601 dates to the user locale: ${input} -> ${expected}`
13. `Blocks orders over 100`
14. `Subscription renews on the billing date`
15. `validateEmail() correctly validates emails`
16. `Trims whitespace from the user name`
17. `A user with a valid subscription is successfully granted access when the subscription is valid`
18. `Applies the weekend surcharge for rides on Saturday and Sunday`
19. `Order ${status} shows ${label} in the timeline`
20. `isEligible flag is set to true for premium members`
21. `Displays the destination address for multi-destination orders`
22. `isValidEmail returns true for ${input}`
23. `Emits an event when the modal closes`
24. `User cannot delete another user's comment`
25. `mapUtmParams handles empty config`
26. `Free shipping applies above the order threshold: ${amount}`
27. `Maps UTM parameters according to the given configuration map for: ${a} ${b}`
28. `handleSubmit is called once on click`
29. `should not throw`
30. `Test that the balance updates`
31. `Cart is empty after checkout completes`
32. `Converts currency to symbol: USD -> $`
33. `Converts currency to symbol: EUR -> €`
34. `Falls back to polling when the ws connection drops`
35. `Removes tracking parameters from a URL before sharing it`
36. `Redirects to https://auth.example.com when auth token expires`
